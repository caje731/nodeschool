var ls = require('./modular_ls.js');

var dir = process.argv[2];
var ext	= process.argv[3];

var display = function(err, list){
	if(!err){
		list.forEach(function(file){
			console.log(file);
		});
	}
	else{
		console.log('There was an error : '+ err);
	}
};

ls(dir,ext,display);